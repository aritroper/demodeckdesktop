class Track {
    constructor(id, name, duration, audioUrl) {
        this.id = id;
        this.name = name;
        this.duration = duration;
        this.audioUrl = audioUrl;
    }
}

export default Track;